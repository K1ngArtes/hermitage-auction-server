# Security
# Session cookies key
SECRET_KEY=your-secret-key-here
# Admin password for protected endpoints (e.g., /admin/bids)
ADMIN_PASSWORD=your-admin-password-here

# Session Configuration
# Session max age in seconds (default: 604800 = 7 days)
SESSION_MAX_AGE=604800
# Admin session max age in seconds (default: 1800 = 30 minutes)
ADMIN_SESSION_MAX_AGE=1800

# Database Configuration
DATABASE_PATH=/app/data/auction.db
DATABASE_BUSY_TIMEOUT=5000

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:80,http://localhost,https://your-domain.com

# Cookie Security
# Set to 'true' for production (HTTPS required), 'false' for local development
COOKIE_SECURE=true

# Logging Configuration
LOG_DIR=/app/logs
# Valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
